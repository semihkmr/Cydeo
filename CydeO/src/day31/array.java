package day31;

public class array {
    public static void main(String[] args) {
        // toCharArray() --> convert a String to a char array, where each element is every character from the  String.

        //  split() : convert a String to a String array, where each element is the sequence of characters between the given separator
  // .sort() --> sort  the elements in ascending order ( smallest to largest )
        // special char -- number -- uppercase (A-Z) -- lowercase(a-z);
 //   Arrays.equals(a,b) -- check if the two different arrays are the same or not
    }
}
